# Server
PORT=3000
NODE_ENV=development

# Google OAuth (for /integrations panel)
# Register the callback URL in Google Cloud Console:
#   http://localhost:3000/integrations/callback
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
